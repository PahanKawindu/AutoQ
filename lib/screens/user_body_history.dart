import 'package:flutter/material.dart';

class UserServiceHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Service History Content Goes Here',
        style: TextStyle(fontSize: 18, color: Colors.grey),
      ),
    );
  }
}
