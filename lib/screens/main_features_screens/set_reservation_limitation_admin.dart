import 'package:flutter/material.dart';

class SetReservationLimitationAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Set Reservation Limit'),
        backgroundColor: const Color(0xFF46C2AF),
      ),
      body: Center(
        child: Text('This is the Set Reservation Limit screen'),
      ),
    );
  }
}
