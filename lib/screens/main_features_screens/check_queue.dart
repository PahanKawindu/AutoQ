import 'package:flutter/material.dart';

class CheckQueue extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: Text('checkQueue'),
        backgroundColor: Color(0xFF34A0A4),
      ),
    );
  }
}
